{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M4 默认能力：主窗口核心权限与本地引擎命令。",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
