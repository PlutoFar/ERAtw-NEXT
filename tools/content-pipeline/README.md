# Content Pipeline

M0 keeps Python tooling out of runtime. This folder is reserved for content audit, reference extraction, validation, and packaging tools.

Planned commands:

- `audit-legacy`: scan read-only ERB/CSV/resources/font/sound sources and emit reports.
- `validate-pack`: validate versioned JSON/YAML authoring content.
- `pack`: compile validated content into a release package.
