[package]
name = "eratw_next_engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ERAtw-NEXT 核心引擎：提供系统状态与地图模型，不依赖 Tauri、不读取磁盘。"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
